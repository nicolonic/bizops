# Google Cloud CLI install + Ads ADC setup
#
# Canonical docs: `autotouch/ads/GOOGLE_ADS_ACCESS.md`
#
# Fresh install (macOS):
curl -s https://sdk.cloud.google.com | bash
#
# After install completes:
~/google-cloud-sdk/install.sh
#
# If gcloud isn't on PATH, use:
~/google-cloud-sdk/bin/gcloud --version
